<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Post Detail — PostHive</title>

    <link rel="stylesheet" href="/style.css">
</head>

<body>

    <h1 class="title">Post Detail</h1>

    <div class="detail-box">
        <h2 class="detail-username"><%= post.username %></h2>
        <h4 class="detail-id">Post ID: <%= post.id %></h4>

        <small style="color:#9aa5b1;">Posted: <%= post.createdAt ? new Date(post.createdAt).toLocaleString() : 'n/a' %> • Likes: <%= post.likes || 0 %></small>

        <p class="detail-content" style="margin-top:12px;"><%= post.content %></p>

        <form method="POST" action="/posts/like/<%= post.id %>" style="display:inline; margin-right:12px;">
            <button type="submit" style="padding:8px 12px; background:#2b6cb0; color:white; border-radius:6px; border:none;">Like</button>
        </form>

        <a href="/posts" class="back-btn">← Back to All Posts</a>
    </div>

</body>
</html>
